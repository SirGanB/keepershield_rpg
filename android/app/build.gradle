plugins {
    id "com.android.application"
    id "kotlin-android"
    id "dev.flutter.flutter-gradle-plugin" // Mantenha se estiver usando versões compatíveis
}

android {
    namespace = "com.example.keepershield_rpg" // Altere para o seu namespace
    compileSdk = 34 // Altere para a versão do SDK desejada
    ndkVersion = flutter.ndkVersion

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_1_8
        targetCompatibility = JavaVersion.VERSION_1_8
    }

    kotlinOptions {
        jvmTarget = JavaVersion.VERSION_1_8
    }

    defaultConfig {
        applicationId = "com.example.keepershield_rpg" // Altere para seu ID de aplicativo
        minSdk = 23 // Altere conforme necessário
        targetSdk = 34 // Altere conforme necessário
        versionCode = flutter.versionCode
        versionName = flutter.versionName
    }

    buildTypes {
        release {
            signingConfig = signingConfigs.debug
        }
    }
}

flutter {
    source = "../.."
}
